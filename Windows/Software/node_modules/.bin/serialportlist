#!/bin/sh
if [ -x "`dirname "$0"`/node" ]; then
  "`dirname "$0"`/node"  "`dirname "$0"`/../serialport2/bin/serialportList.js" "$@"
  ret=$?
else 
  node  "`dirname "$0"`/../serialport2/bin/serialportList.js" "$@"
  ret=$?
fi
exit $ret
